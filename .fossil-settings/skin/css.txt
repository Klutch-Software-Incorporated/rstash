/* Fossil Skin
   - System light/dark via prefers-color-scheme
   - Palette derived from your Flutter theme extension
*/

:root{
  /* Feature colors (light) */
  --cool:   #4DB6AC;
  --warm:   #FF8A65;
  --calm:   #9575CD;
  --bold:   #FFD54F;
  --fresh:  #F06292;

  --success:#4CAF50;
  --error:  #E53935;
  --streak: #FF9800; /* Use as primary accent */

  /* Surfaces (light) */
  --coolSurface:  rgba(77,182,172,0.10);
  --warmSurface:  rgba(255,138,101,0.10);
  --calmSurface:  rgba(149,117,205,0.10);
  --boldSurface:  rgba(255,213,79,0.10);
  --freshSurface: rgba(240,98,146,0.10);

  /* Neutrals (light) */
  --bg: #FBFAF8;
  --surface: #F4F1EC;
  --card: #FFFFFF;
  --text: #1F1C18;
  --muted: #6B645E;
  --border: rgba(31,28,24,0.10);

  --shadow: 0 10px 24px rgba(0,0,0,0.08);
  --radius: 16px;

  color-scheme: light dark;
}

@media (prefers-color-scheme: dark){
  :root{
    /* Feature colors (dark) */
    --cool:   #80CBC4;
    --warm:   #FFAB91;
    --calm:   #B39DDB;
    --bold:   #FFE082;
    --fresh:  #F48FB1;

    --success:#81C784;
    --error:  #EF5350;
    --streak: #FFB74D;

    /* Surfaces (dark) */
    --coolSurface:  rgba(128,203,196,0.15);
    --warmSurface:  rgba(255,171,145,0.15);
    --calmSurface:  rgba(179,157,219,0.15);
    --boldSurface:  rgba(255,224,130,0.15);
    --freshSurface: rgba(244,143,177,0.15);

    /* Neutrals (dark) */
    --bg: #0F0D0B;
    --surface: #171411;
    --card: #1C1915;
    --text: #F2ECE6;
    --muted: #B7AEA6;
    --border: rgba(242,236,230,0.10);

    --shadow: 0 14px 30px rgba(0,0,0,0.55);
  }
}

/* Base */
html, body { height: 100%; }
body{
  margin: 0;
  padding: 0 18px;
  background: var(--bg);
  color: var(--text);
  font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, Arial, "Noto Sans", "Liberation Sans", sans-serif;
  font-size: 16px;
  line-height: 1.45;
  -webkit-text-size-adjust: 100%;
  text-rendering: optimizeLegibility;
}

a{
  color: var(--streak);
  text-decoration: none;
}
a:hover{ text-decoration: underline; }

/* Layout container feel */
.header{
  margin: 18px auto 10px;
  max-width: 1100px;
}
.mainmenu, .submenu, .content, .footer{
  max-width: 1100px;
  margin-left: auto;
  margin-right: auto;
}

/* Top bar */
.om-topbar{
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 10px 12px;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
}

.om-brand{
  display: flex;
  align-items: center;
  gap: 12px;
  min-width: 0;
}
.om-logo{
  width: 40px;
  height: 40px;
  border-radius: 999px;
  background: radial-gradient(circle at 30% 30%, var(--streak), color-mix(in srgb, var(--streak), black 25%));
  box-shadow: 0 10px 18px rgba(0,0,0,0.18);
  flex: 0 0 auto;
}
.om-title{
  display: flex;
  flex-direction: column;
  min-width: 0;
}
.om-title .om-app{
  font-size: 22px;
  font-weight: 750;
  letter-spacing: 0.2px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.om-title .om-page{
  font-size: 13px;
  color: var(--muted);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* Login/status pill */
.om-status a{
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 8px 12px;
  border-radius: 999px;
  background: var(--card);
  border: 1px solid var(--border);
  color: var(--text);
  text-decoration: none;
}
.om-status a:hover{
  text-decoration: none;
  border-color: color-mix(in srgb, var(--streak), var(--border) 60%);
}

/* Main menu -> soft pill buttons */
.mainmenu{
  margin: 12px auto 10px;
  padding: 10px;
  background: transparent;
  border: 0;
  overflow-x: auto;
  white-space: nowrap;
}
.mainmenu a{
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 14px;
  margin-right: 10px;
  border-radius: 999px;
  background: var(--card);
  border: 1px solid var(--border);
  color: var(--muted);
  text-decoration: none;
}
.mainmenu a:hover{
  color: var(--text);
  border-color: color-mix(in srgb, var(--streak), var(--border) 60%);
}
.mainmenu a.active{
  color: var(--text);
  border-color: color-mix(in srgb, var(--streak), var(--border) 35%);
  box-shadow: 0 10px 18px rgba(0,0,0,0.10);
}
.mainmenu a#hbbtn{
  font-weight: 700;
}

/* Submenu */
.submenu{
  margin: 0 auto 14px;
  padding: 10px 10px;
  background: transparent;
  border: 0;
}
.submenu a, .submenu label{
  display: inline-flex;
  padding: 8px 10px;
  border-radius: 12px;
  color: var(--muted);
  text-decoration: none;
}
.submenu a:hover, .submenu label:hover{
  color: var(--text);
  background: var(--surface);
  border: 1px solid var(--border);
}

/* Content as “card” */
.content{
  margin: 0 auto 18px;
  padding: 16px 16px;
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  color: var(--text);
}
.content h1, .content h2, .content h3{
  color: var(--text);
}

/* Tables (timeline/reports/etc.) */
table{
  border-collapse: separate;
  border-spacing: 0;
}
table.report{
  width: 100%;
  border-radius: var(--radius);
  border: 1px solid var(--border);
  overflow: hidden;
  background: var(--card);
}
.report th{
  background: var(--surface);
  color: var(--muted);
  font-weight: 700;
}
.report td, .report th{
  padding: 10px 12px;
  border-bottom: 1px solid var(--border);
}
.report tr:last-child td{ border-bottom: 0; }
.report thead+tbody tr:hover{
  background: color-mix(in srgb, var(--coolSurface), var(--card) 60%) !important;
}

/* Ticket display cells */
td.tktDspValue{
  background: var(--surface);
  border: 1px solid var(--border);
}
td.tktDspValue pre{ white-space: pre-wrap; }

/* Timeline date emphasis */
div.timelineDate{
  color: var(--muted);
  font-weight: 800;
}

/* Footer */
.footer{
  margin: 0 auto 24px;
  padding: 12px 14px;
  color: var(--muted);
  font-size: 13px;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  background: var(--surface);
}

/* Mobile */
@media (max-width: 700px){
  body{ padding: 0 10px; }
  .om-title .om-app{ font-size: 18px; }
  .content{ padding: 14px; }
}



/* --------------------------------------------
   Fossil Timeline (Modern View) contrast fixes
   -------------------------------------------- */

/* Force readable text in modern timeline cells */
td.timelineModernCell,
td.timelineModernCell *,
span.timelineModernComment,
span.timelineModernDetail{
  color: var(--text) !important;
}

/* Keep links “accented” */
td.timelineModernCell a,
span.timelineModernDetail a{
  color: var(--streak) !important;
}
td.timelineModernCell a:hover,
span.timelineModernDetail a:hover{
  text-decoration: underline;
}

/* Timeline table spacing + card feel */
table.timelineTable{
  border-collapse: separate !important;
  border-spacing: 0 12px !important;
  width: 100%;
}

/* The event row “card” background */
table.timelineTable td.timelineModernCell{
  background: var(--card) !important;
  border: 1px solid var(--border) !important;
  border-radius: var(--radius) !important;
  box-shadow: 0 10px 18px rgba(0,0,0,0.10);
  padding: 12px 14px !important;
}

/* Selected row: use your warm surface tint instead of Fossil’s pale yellow */
table.timelineTable tr.timelineSelected td.timelineModernCell{
  background: color-mix(in srgb, var(--warmSurface), var(--card) 75%) !important;
  border-color: color-mix(in srgb, var(--warm), var(--border) 60%) !important;
}

/* Left-side time column should remain readable */
td.timelineTime{
  color: var(--streak) !important;
  font-variant-numeric: tabular-nums;
}

/* Date headings */
div.timelineDate{
  color: var(--muted) !important;
}

/* --------------------------------------------
   Form controls (these often stay “light”)
   -------------------------------------------- */
input, select, textarea, button{
  color: var(--text);
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 6px 8px;
}
input:focus, select:focus, textarea:focus{
  outline: none;
  border-color: color-mix(in srgb, var(--streak), var(--border) 40%);
  box-shadow: 0 0 0 3px color-mix(in srgb, var(--warmSurface), transparent 40%);
}

/* ======================================================================
   Fossil /chat styling
   ====================================================================== */

/* Use the same centered column as the rest of the site */
body.chat .content,
body.page-chat .content{
  max-width: 1100px !important;
  margin-left: auto !important;
  margin-right: auto !important;
  box-sizing: border-box !important;
}

/* Make chat major blocks fill the centered column */
body.chat #chat-input-area,
body.chat #chat-messages-wrapper,
body.chat #chat-user-list-wrapper,
body.chat #chat-preview,
body.chat #chat-config,
body.page-chat #chat-input-area,
body.page-chat #chat-messages-wrapper,
body.page-chat #chat-user-list-wrapper,
body.page-chat #chat-preview,
body.page-chat #chat-config{
  width: 100% !important;
  margin-left: 0 !important;
  margin-right: 0 !important;
  box-sizing: border-box !important;
}

/* Style the blocks as cards */
body.chat #chat-input-area,
body.page-chat #chat-input-area{
  padding: 12px !important;
  background: var(--card) !important;
  border: 1px solid var(--border) !important;
  border-radius: var(--radius) !important;
  box-shadow: var(--shadow) !important;
  margin-bottom: 12px !important;
}

body.chat #chat-messages-wrapper,
body.page-chat #chat-messages-wrapper{
  padding: 12px !important;
  background: var(--card) !important;
  border: 1px solid var(--border) !important;
  border-radius: var(--radius) !important;
  box-shadow: var(--shadow) !important;
  margin-bottom: 12px !important;
}

/* Ensure everything in chat defaults to readable text */
body.chat #chat-input-area,
body.chat #chat-messages-wrapper,
body.chat #chat-preview,
body.chat #chat-config,
body.chat #chat-user-list-wrapper,
body.chat #chat-input-area *,
body.chat #chat-messages-wrapper *,
body.chat #chat-preview *,
body.chat #chat-config *,
body.chat #chat-user-list-wrapper *{
  color: var(--text);
}

/* Input row: flex layout so the field grows */
body.chat #chat-input-line-wrapper,
body.page-chat #chat-input-line-wrapper{
  display: flex !important;
  align-items: center !important;
  gap: 10px !important;
  width: 100% !important;
  box-sizing: border-box !important;
}

/* Input fields: fill available width and match theme */
body.chat #chat-input-field-single,
body.chat #chat-input-field-multi,
body.chat #chat-input-field-x,
body.chat .chat-input-field,
body.page-chat #chat-input-field-single,
body.page-chat #chat-input-field-multi,
body.page-chat #chat-input-field-x,
body.page-chat .chat-input-field{
  flex: 1 1 auto !important;
  width: 100% !important;
  min-width: 0 !important;
  box-sizing: border-box !important;

  padding: 10px 12px !important;
  border-radius: 14px !important;
  border: 1px solid var(--border) !important;
  background: var(--surface) !important;
  color: var(--text) !important;
}

/* Placeholder contrast */
body.chat .chat-input-field::placeholder,
body.page-chat .chat-input-field::placeholder{
  color: color-mix(in srgb, var(--muted), transparent 15%);
}

/* Buttons cluster */
body.chat #chat-buttons-wrapper,
body.page-chat #chat-buttons-wrapper{
  flex: 0 0 auto !important;
  display: inline-flex !important;
  align-items: center !important;
  gap: 8px !important;
}

body.chat .cbutton,
body.page-chat .cbutton{
  background: var(--surface) !important;
  border: 1px solid var(--border) !important;
  border-radius: 12px !important;
  padding: 6px 10px !important;
  cursor: pointer;
  user-select: none;
}

/* Load toolbar buttons (Previous 50 / All previous) */
body.chat #load-msg-toolbar button,
body.page-chat #load-msg-toolbar button{
  background: var(--surface) !important;
  border: 1px solid var(--border) !important;
  color: var(--text) !important;
  border-radius: 12px !important;
  padding: 8px 12px !important;
}

/* Message widget header/tab: Fossil sets inline bg-color -> override */
body.chat .message-widget-tab,
body.page-chat .message-widget-tab{
  background-color: var(--surface) !important;
  border: 1px solid var(--border) !important;
  color: var(--muted) !important;
  border-radius: 12px !important;
  padding: 6px 10px !important;
}

/* Username emphasis inside the tab */
body.chat .message-widget-tab .xfrom,
body.page-chat .message-widget-tab .xfrom{
  color: var(--text) !important;
  font-weight: 700;
}

/* Ensure tab children inherit */
body.chat .message-widget-tab span,
body.chat .message-widget-tab * ,
body.page-chat .message-widget-tab span,
body.page-chat .message-widget-tab *{
  color: inherit !important;
}

/* Message bodies: use surface bubble styling if Fossil uses message-widget-content */
body.chat .message-widget-content,
body.page-chat .message-widget-content{
  background: var(--surface) !important;
  border: 1px solid var(--border) !important;
  border-radius: 14px !important;
  padding: 10px 12px !important;
}

/* Links inside chat */
body.chat a,
body.page-chat a{
  color: var(--streak);
}
body.chat a:hover,
body.page-chat a:hover{
  text-decoration: underline;
}



/* -----------------------------
   Table background fixes.
   ----------------------------- */


table.timelineTable tr.timelineSelected{
  background-color: var(--timeline-selected) !important;
  border: 0 !important;
  box-shadow: none !important;
}

/* If the cells themselves need to inherit */
table.timelineTable tr.timelineSelected > td{
  background-color: inherit !important;
}
